<div class="card" style="height: 30rem;width: 16rem;margin-left:  28rem;background-color: #DEBC99;">
<div class="card-header" style="text-align: center;background-color: #DCD0BA"><h1>Sign up</h1>
<% if @user.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@user.errors.count, "error") %> prohibited this @user from being saved:</h2>
    <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
  <div class="card-body"><%= render 'form', user: @user %></div>

  <div class="card-body"><%= link_to 'Back', users_path %></div>
   <%= link_to "login", new_session_path %>
  


    
 